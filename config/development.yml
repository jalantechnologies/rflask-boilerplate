logger:
  transports: ['console']

mongodb:
  uri: 'mongodb://localhost:27017/frm-boilerplate-dev'

temporal:
  server_address: 'localhost:7233'

sms:
  enabled: true

is_server_running_behind_proxy: false

public:
  authenticationMechanism: 'EMAIL' #or 'PHONE'
  default_otp:
    enabled: true
    code: '1234'

firebase:
  enabled: true

email:
  sendgrid:
    api_key: "SENDSENDGRID_API_KEY"
    default_sender:
      email: "sarthak.gaikwad@bettrhq.com"
      name: "Better Software"